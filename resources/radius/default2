if("%{sql:SELECT SUM(max_usage)  FROM users  WHERE username = '%{User-Name}'  }" <=  "%{sql:SELECT  SUM(user_graph.total)  FROM user_graph INNER JOIN users ON users.id = user_graph.user_id WHERE users.username = '%{User-Name}'   }"){
                	update reply {
                 	    &Reply-Message := "Bandwidth Limit"
                      	&Framed-Pool := "Bandwidth Limit"
                 	}
                 	reject
      }
